import {
  useState,
  useCallback,
} from 'react';
import {
  FiMenu,
} from 'react-icons/fi';
import {
  Container,
  MenuContent,
} from '../../styles/components/menu';
export const Menu: React.FC = ({ children }) => {
  return (
    
  );
};

export default Menu;